//this might not be possible and it was breaking everything to its been put in timeout

// import java.io.IOException;

// public class KeyCheckManager {

//     /**
//      * Detects if the backspace key is pressed.
//      * 
//      */
//     public static boolean isKeyPressed(){
//         try {
//             int key = System.in.read();
//             System.err.println(key == 8 || key == 127);
//             // Detect backspace (ASCII code 8 or 127)
//             return key == 8 || key == 127;
//         } catch (IOException ex) {
//             return false;
//         }
//         return
//     }
// }
